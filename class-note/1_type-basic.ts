// JS 문자열 선언
// var str = 'hello';

// TS 문자열 선언
const str: string = 'hello';

// TS 숫자
const num: number = 10;

// TS 배열
const arr1: Array<number> = [1, 2, 3];
const arr2: string[] = ['a', 'b', 'c'];

// TS 튜플
const address: [number, string] = [1, 'a'];

// TS 객체
const obj: { name: string; age: number } = { name: 'John', age: 20 };

// TS 진위값
const truth: boolean = true;
